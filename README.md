A proper README is TBD.

For now, just try the following invocation on your command line:

```
ffd 💩 [jcalizo] $ python ./ffd.py
```

You might need to `pip install` some additional Python libraries.